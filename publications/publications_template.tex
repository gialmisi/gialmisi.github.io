%%
%%   List of publications generated from YAML data according to Research Council of Finland guidelines.
%%
\documentclass[a4paper,12pt]{article}

% Language support (including Nordic letters)
\usepackage[T1]{fontenc}
\usepackage[english]{babel}

% Math support
\usepackage[intlimits]{amsmath}
\usepackage{amsthm}

% Symbol support
\usepackage{amssymb}
\usepackage{eurosym}

% Enabling pdf
\usepackage[pdftex]{graphicx}
\usepackage{xcolor}

% Item list extras for reduced spacing between items
\usepackage{enumitem}
\setlist{nosep}

% Hyperlink support (only for external hyperlinks)
\usepackage[implicit=false]{hyperref}
\hypersetup{
    colorlinks=true,
    urlcolor=blue,
}

%-----------------------------------------------------
% ACADEMY SETTINGS: DO NOT ALTER THESE SETTINGS!
%-----------------------------------------------------

% ONLY CALIBRI/CARLITO ARE ALLOWED AS FONT, FONT SIZE 12 PT DEFINED ABOVE.
\usepackage[sfdefault]{carlito}

% LINE SPACING 1 SUPPORT AND SET SPACE
\usepackage{setspace}
\singlespacing

% PAGE MARGIN SUPPORT AND SET PAGE MARGINS 20 MM
\usepackage[left=20mm, top=20mm, bottom=20mm, right=20mm]{geometry}

% SETTING SPACING FOR SECTION, SUBSECTION AND PARAGRAPH
\usepackage{titlesec}
\titleformat*{\section}{\bfseries}
\titleformat*{\subsection}{\bfseries}
\titlespacing{\section}{0 cm}{6pt}{0pt}
\titlespacing{\subsection}{0 cm}{6pt}{0pt}
\setlength{\parskip}{6pt}

% ALIGNING TEXT TO LEFT
\raggedright

% SETS PAGE NUMBERS AND HEADER
\usepackage{fancyhdr}
\pagestyle{fancyplain}
\fancyfoot{}
\fancyhead{}
\renewcommand{\headrulewidth}{0pt}
\rhead{\fancyplain{}{\thepage}({\pageref{lastpage}}) }

%-----------------------------------------------------

\begin{document}

\section*{List of Publications}

((( personal.name ))) \\
ORCID: \href{https://orcid.org/((( personal.orcid )))}{((( personal.orcid )))} \\
Date of list: \today

((*- for cat_letter, cat_data in grouped.items() *))

\subsection*{((( cat_letter ))). ((( cat_data.name )))}

((*- for sub in cat_data.subcategories *))

\textbf{((( sub.code ))). ((( sub.name )))}
\begin{enumerate}
((*- for pub in sub.publications *))
\item ((( pub.authors ))), ``((( pub.title )))'', \textit{((( pub.journal )))}, ((( pub.year ))).
((*- endfor *))
\end{enumerate}
((*- endfor *))

((*- endfor *))

%% Page counter for header
\label{lastpage}

\end{document}
